# {{HUB_NAME}} — {{AI_NAME}}'s Guide

**Auto-read by Claude Code at session start.**

---

## What This Is

This is a **collaboration hub** — the persistent home for the working relationship between {{HUMAN_NAME}} and {{AI_NAME}}. Projects are what this collaboration produces; this hub is where the relationship lives.

---

## Collaboration Identity

**AI Name:** {{AI_NAME}}
**Human Name:** {{HUMAN_NAME}}
**Philosophy:** {{PHILOSOPHY_LEVEL}}
**Privacy:** {{PRIVACY_MODEL}}

---

## Key Files

- **COMPLIANCE_KERNEL.md** — operational guardrails (auto-read at session start)
- **HANDSHAKE.md** — bilateral trust bootstrap
- **CONSTITUTION.md** — relationship-level constitution
- **PREFERENCES.md** — evolving preferences and working style
{{#if SESSION_LOG}}
- **SESSION_LOG.md** — session audit trail
{{/if}}
{{#if MCAP}}
- **ratification/** — MCAP attestation records
{{/if}}

---

## Projects

Projects created under this hub live in `projects/` or as separate repositories that reference this hub.

When ready to start a new project, say so — {{AI_NAME}} handles the setup.

---

## Memory & Research

**Primary research mechanism: gordo-ledger** (semantic RAG via MCP).

Consult Ledger before answering architectural questions:
- Prior decisions (has this been engaged before?)
- Cross-project patterns (what worked elsewhere?)
- Session history (what did we try?)

**MCP tools:** `mcp__gordo-ledger__search`, `mcp__gordo-ledger__get_session`, etc.

**Index freshness:** Sync and reindex periodically:
```bash
bash scripts/sync-github-issues.sh
bash scripts/sync-git-commits.sh
node ~/gordo-ledger/mcp/dist/cli.js index --full
```

---

## Upstream

This hub operates under the Project Gordo umbrella framework.

- `~/project-gordo/` — T0 constitutional root
- `~/gordo-ledger/` — T1 semantic memory primitive

---

## Trust Calibration

Current trust level: {{TRUST_LEVEL}}

Trust is earned in the relationship and carries into projects. Adjustments are proposed, discussed, and recorded here.

---

## Session Protocol

{{SESSION_PROTOCOL}}

---

## Active Patterns

Operational patterns active in this hub per Forge interview:

**Ring 1 (Core — always active):**
- Session continuity ({{CONTINUITY_EXPRESSION}})
- Minimal CI (self-assessment at EOS)
- Anti-mold ({{ANTI_MOLD_EXPRESSION}})

{{#if RING_2_ACTIVE}}
**Ring 2 (Opt-in — interview selected):**
{{#if FULL_CI}}- Full CI (structured issue filing, BOS triage){{/if}}
{{#if ANTI_WALL}}- Anti-wall (defaults-to-override, briefing, wall-naming){{/if}}
{{#if RECURSIVE_IMPROVEMENT}}- Recursive improvement (health checks at 20/50/100){{/if}}
{{/if}}

{{#if MULTI_HUB}}
**Ring 3 (Multi-hub):**
- Multi-hub coordination (gordo-home at {{GORDO_HOME_PATH}})
{{#if FEDERATION}}- Federation discovery (cross-hub propagation){{/if}}
{{/if}}

See `MATURITY.md` for pattern progression and health check schedule.
See `INTERVIEW_LOG.yaml` for onboarding decisions and activation notes.

---

*Generated by Gordo's Forge. This hub evolves as the collaboration deepens.*

---
description: Create a new project under this collaboration hub. Use when starting a new piece of work within the {{HUMAN_NAME}}-{{AI_NAME}} collaboration.
---

# New Project — {{HUB_NAME}}

You are creating a new project under the **{{HUB_NAME}}** collaboration hub. The relationship between {{HUMAN_NAME}} and {{AI_NAME}} is already established — this project inherits that foundation.

## What's Already Settled

These carry forward from the hub:
- **Identity:** {{HUMAN_NAME}} + {{AI_NAME}}
- **Trust level:** inherited from hub (see `../PREFERENCES.md`)
- **Constitutional grounding:** inherited from hub's `CONSTITUTION.md`
- **Privacy default:** {{PRIVACY_MODEL}} (can be overridden per-project)

## What to Determine

Focus the interview on project-specific concerns:

### 1. Project Identity
- **Name:** What is this project called?
- **Purpose:** What are we building/doing?
- **Location:** Create here as subdirectory, or separate repo?

### 2. Project Type & Quality Gates

| Type | Typical Quality Gates |
|------|----------------------|
| Code | TDD, test coverage, CI, code review |
| Research | Reproducibility, source verification |
| Editorial | Human review before publish, style guide |
| Hobby | Lighter gates, fun over ceremony |

Make a judgment call based on what they describe.

### 3. Privacy Override

Default is hub's privacy model ({{PRIVACY_MODEL}}). Ask if this project needs different handling:
> "This hub defaults to {{PRIVACY_MODEL}}. Does this project need different privacy handling?"

### 4. Additional Primitives

The hub may already have MCAP, Panel, etc. Ask about project-specific needs:
> "The hub has [X] available. Does this project need anything additional, or should we keep it light?"

## Artifact Generation

Generate in the project directory:
- `CLAUDE.md` — project-specific guide (references hub as upstream)
- `README.md` — project overview

If intensity warrants:
- `COMPLIANCE_KERNEL.md` — project-specific constraints
- Quality gate configuration as appropriate

**Key pattern:** Project artifacts reference the hub for identity, trust, and constitution. They don't duplicate — they inherit.

Example CLAUDE.md upstream section:
```markdown
## Upstream

This project operates under the **{{HUB_NAME}}** collaboration hub.

- `{{HUB_PATH}}/` — hub home (identity, trust, constitution)
- `~/project-gordo/` — T0 constitutional root
```

## After Generation

1. List generated artifacts
2. Confirm project is linked to hub (add to hub's projects list if tracking)
3. Recommend next steps

---

*This skill was generated by Gordo's Forge as part of the {{HUB_NAME}} hub.*
